`// file cannot start with backtick? patch includes. need proper content` wait can't include ` yet. Need patch properly. abort. Need re-run without stray. Try again. Need patch format. Let's craft text.*** End Patch

